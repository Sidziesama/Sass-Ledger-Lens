These seven synthetic datasets are copied unchanged from Sidziesama/Sass-Ledger-Lens, reliability branch commit 6288a94. Tests translate CSV fields into the product schema while preserving currencies and duplicate IDs, then call src.workflow.run_workflow. Assertions cover our supported workflow safeguards, not every requirement of the source benchmark. No reliability implementation is imported.
